#!/bin/bash

# 测试脚本：立即执行一次新闻推送任务

echo "=========================================="
echo "测试：立即执行新闻推送任务"
echo "=========================================="

# 切换到项目根目录
cd "$(dirname "$0")/.."

# 设置立即执行标志
export RUN_IMMEDIATELY=true

echo "启动定时任务调度器（测试模式）..."
python3 src/scheduler.py
